{
  "_comment": "QuietContext uses QUIET_CONTEXT_PROJECT_DIR as its workspace boundary.",
  "mcpServers": {
    "quietcontext": {
      "command": "node",
      "args": ["${CODEX_PLUGIN_ROOT}/start.mjs"],
      "env": {
        "QUIET_CONTEXT_PROJECT_DIR": "${workspaceFolder}"
      }
    }
  }
}
